'use client'
import { FormEvent, useState } from 'react'
export default function AdminLoginPage(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();await fetch('/api/auth/csrf');const csrf=document.cookie.match(/(?:^|;\s*)frankai_csrf=([^;]+)/)?.[1]||'';const r=await fetch('/api/auth/login',{method:'POST',credentials:'include',headers:{'content-type':'application/json','x-csrf-token':csrf},body:JSON.stringify({email,password})});if(!r.ok){setError('Administrator credentials are incorrect.');return}const me=await fetch('/api/auth/me',{credentials:'include'});const data=await me.json();if(data.user?.role!=='ADMIN'){await fetch('/api/auth/logout',{method:'POST',credentials:'include',headers:{'x-csrf-token':csrf}});setError('Administrator credentials are incorrect.');return}location.href=data.mfaRequired&&!data.mfaAuthenticated?'/admin/mfa':'/admin'}return <main className="page-shell"><section className="content-section"><p className="eyebrow">Restricted area</p><h1>Administrator sign in.</h1><form onSubmit={submit} className="contact-form"><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p role="alert">{error}</p>}<button>Sign in</button></form></section></main>}
