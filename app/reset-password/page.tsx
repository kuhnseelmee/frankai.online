'use client'
import { FormEvent, useState } from 'react'
export default function ResetPasswordPage() { const [password, setPassword] = useState(''); const [message, setMessage] = useState('Choose a new password.'); async function submit(event: FormEvent) { event.preventDefault(); const token = new URLSearchParams(location.search).get('token') || ''; const response = await fetch('/api/auth/password/reset', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ token, password }) }); const data = await response.json(); setMessage(response.ok ? 'Password changed. Sign in again.' : data.error || 'This reset link is invalid or expired.') } return <main className="page-shell"><section className="content-section"><p className="eyebrow">Password recovery</p><h1>Reset your password.</h1><p>{message}</p><form onSubmit={submit} className="contact-form"><label>New password<input required minLength={12} type="password" value={password} onChange={e => setPassword(e.target.value)} /></label><button>Change password</button></form></section></main> }
